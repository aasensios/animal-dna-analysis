import { MonomersValidationDirective } from './monomers-validation.directive';

describe('MonomersValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new MonomersValidationDirective();
    expect(directive).toBeTruthy();
  });
});
