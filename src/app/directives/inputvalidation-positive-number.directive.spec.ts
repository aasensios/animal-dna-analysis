import { InputvalidationPositiveNumberDirective } from './inputvalidation-positive-number.directive';

describe('InputvalidationPositiveNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new InputvalidationPositiveNumberDirective();
    expect(directive).toBeTruthy();
  });
});
