import { InputvalidationNegativeNumberDirective } from './inputvalidation-negative-number.directive';

describe('InputvalidationPositiveNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new InputvalidationNegativeNumberDirective();
    expect(directive).toBeTruthy();
  });
});
