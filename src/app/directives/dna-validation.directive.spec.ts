import { DnaValidationDirective } from './dna-validation.directive';

describe('DnaValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DnaValidationDirective();
    expect(directive).toBeTruthy();
  });
});
